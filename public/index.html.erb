<!DOCTYPE html>
<html>
<head>
  <link href='//fonts.googleapis.com/css?family=Imprima' rel='stylesheet' type='text/css'>
  <link href="/css/normalize.min.css" rel="stylesheet" type="text/css">
  <link href="/css/default.css" rel="stylesheet" type="text/css">
  <title>The Foods of Southern India</title>
</head>
<body>


<header>The Foods of Southern India</header>

<main>
  <% file = (File.expand_path("../config/paragraphs.txt", __FILE__)) %>
  <% text = File.open(file).read %>
  <p>
    <% array = text.split(/_____/)%>
    <% array.each do |paragraph| %>
       <p><%= paragraph %></p>
    <% end %>
  </p>

  <img src="/images/food1.jpg">

  <dl>
    <% menu = Menu.new %>
    <% menu.items.each do |item| %>
      <% if item.day == Date.today.strftime("%A").downcase || item.day == "all" %>
        <dt><%= item.name %> - <%= item.price %></dt>
        <dd><%= item.description %></dd>
        <dd><img src="/images/<%= item.pic %>"></dd>
      <% end %>
    <% end %>
  </dl>
</main>

<footer>
  <address>
    123 Main Street
    Boulder, CO 80301
    <a href="mailto:bob@example.com">Contact Us</a>
  </address>
  &copy; <%= Time.now.year %>
</footer>

</body>
</html>
